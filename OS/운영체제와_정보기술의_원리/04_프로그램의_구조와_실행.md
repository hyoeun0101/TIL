# 인터럽트

인터럽트가 발생하면, CPU는 실행 중이던 프로세스를 멈추고, 프로세스의 문맥을 PCB(커널의 데이터 영역에 존재)에 저장한 다음   
인터럽트의 핸들러에서 해당 처리 코드를 찾아 처리를한다.

인터럽트 처리 중 또 다른 인터럽트 발생?   
데이터의 일관성 때문에 원칙적으로 허용되지 않지만, 인터럽트에도 우선순위를 부여하여 현재 처리 중이던 인터럽트 수행 지점을 저장 후    
우선순위가 높은 인터럽트부터 처리한다.

# 시스템 콜
