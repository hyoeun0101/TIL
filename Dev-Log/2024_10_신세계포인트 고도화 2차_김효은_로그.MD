## 🔴  2024-09 ~ 2025-01 (2월 오픈)
### 🟡 우선순위
계정 > 해외 > 전체(본인인증) > 로그인 2factor > 신규기기 > 전체(캡챠)
- 계정과 해외 동시 차단-> 본인인증 -> 성공 후 해외 차단 본인인증 실행.


### 🟡 deviceNo 넣은 곳
|기능|url|api-Controller|ext-Controller|
|----|----|---------|--------|
|ID/PWD 로그인|`/mber/v1.0.0/login`|MberLoginController|MberLoginController|
|자동 로그인|`/webapp/v1.0.0/autoLogin`|AutoLoginController|AutoLoginController|
| sns 로그인|`/mber/v1.0.0/login`|MberLoginController|MberLoginController|
|생체 로그인|`/webapp/v1.0.0/bioLogin`|BioLoginController|AutoLoginContoller|
|sso 로그인|`/mber/v1.0.0/ssoTokenCheck`|MberLoginController|MberLoginController|





### 🟡 해야할 것
- ⬜ shinpo-api > GroupLoginServiceImpl > checkGroupSsoToken => validation 체크 try 문 밖으로 빼기

- ⬜ ext-createSimpleCertification에 일단 controller 단에 setChngPromId로 값 넣어놨는데 진짜 원인은 @RequestBody 필드로 상속값이 안들어오는 문제임.
- ☑️ ~~@Transactional 공부~~
- ⬜ Enum 활용하기 공부
<br><br><br><br>

---


### 🟡 고민
- @SuperBuilder - 도대체 @SuperBuilder 어떻게 사용하는 것??? 
- 지금 에러 나는 부분 : CommonModel에 @SuperBuilder를 작성하였는데 기존에 CommonModel을 사용하던 곳에서 에러가 발생함. 해당 클래스에는 @SuperBuilder가 없기 때문인데...

<br><br><br>

## 🔴Log-BO

### 🟡 LocalDateTime 필드 json으로 변환

1. `@DataTimeFormat(pattern="yyyy-MM-dd HH:mm:ss")`
	
	- @RequestParam, @ModelAttribute일 경우 필드에 붙이기.

2. `JsonFormat(shape = JsonFormat.Shap.STRING, pattern = "yyyy-MM-dd HH:mm:ss")`
	
	- @RequestBody, @ResponseBody일 경우 필드에 붙이기.
<br>

---
### 🟡 Java의 LocalDateTime과 DB의 timestamp 매핑시 에러 발생
- 원래 LocalDateTime과 timestamp는 자동 매핑이 됨. 오류가 발생한 이유는 mybatis를 작성할 때 restrictDt 필드(LocalDateTime 타입)를 `restrictDt != ""` 이렇게 작성했기 떄문!! 

```
에러 메세지 : IllegalArgumentException: invalid comparison:java.time.LocalDateTime and java.lang.String
```

<br>

---
### 🟡 @JsonSerialize(using = LocalDateTimeSerializer.class) 은 언제 사용하는가?
- 직렬화 방식을 커스터마이징할 때 사용한다.

```java
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.ser.std.ToStringSerializer;

public class Example {
    
    // 커스텀 직렬화 클래스 지정
    @JsonSerialize(using = ToStringSerializer.class)
    private Long id;

    // 직접 정의한 커스텀 직렬화기 사용 가능
    @JsonSerialize(using = CustomDateSerializer.class)
    private Date createdDate;

    // getters, setters
}
```

<br>

---
### 🟡 EXT에서 @RequestBody로 필드 받을 때 CommonModel 상속 부분에 값이 안들어옴. 그 이유는?__

- 되는데?   
RequestBody는 어떻게 바인딩되는걸까?
- `HttpMessageConverter`를 사용하여 바인딩이 이루어진다.
- Jackson 라이브러리의 `MappingJackson2HttpMessageConverter`가 JSON 데이터를 자바 객체로 변환해준다.

	
- RequestBody는 기본생성자와 set 메소드로 바인딩된다!!



- 참고
	-  https://strong-park.tistory.com/entry/RequestBody%EB%8A%94-%EC%96%B4%EB%96%BB%EA%B2%8C-%EB%B0%94%EC%9D%B8%EB%94%A9-%EB%90%98%EB%8A%94%EA%B1%B8%EA%B9%8C-with-%EB%94%94%EB%B2%84%EA%B9%85-%EA%B3%BC%EC%A0%95

	- Dispatcher 제대로 알자 : https://mangkyu.tistory.com/18

<br>

---
### 🟡 @SuperBuilder 사용법

- 참고 : https://second-brain.tistory.com/12
- 부모의 필드도 builder 패턴으로 값을 생성하고 싶을 때 사용한다.
- 자식 클래스, 부모 클래스에 @SuperBuilder를 선언해야 한다. 

- @SuperBuilder, @NoArgsConstructor : @AllArgsContructor은 필요없나봄.
- @Builder는 자동으로 allArgs 생성자 만듦. 


<br>

---
### 🟡 Interval 사용법
- INTERVAL : 시간 간격을 나타내는 데이터 타입.
- 시간 계산하기
```sql
-- 1시간 더하기
select NOW() + interval '1hour';
-- 1분 빼기
select NOW() - interval '1minute'
```

<br>

---
### 🟡 sql NOW()가 이상하게 찍힌다...
- 프로그램으로 돌려서 now()하면 제대로 된 값 들어가는데???

- now()가 트랜잭션 하나 잡히면 고정이었음!!!!!!!대박적.ㅋ
- https://velog.io/@clevelog/DataGrippostgresql-timezone-%EC%84%A4%EC%A0%95



- nullpointException 발생하는 경우 : DB에서 send delete하고 로그 생성 api 쏘면 발생.

<br>

---
### 🟡 Mybais에서 Interval에 변수 사용법 - CAST 활용
1. 내가 원하는 동작    
	: 5minute의 5를 변수로 사용하고 싶다.
```sql
SELECT COUNT(*)
FROM 2FACTOR_SEND SEND
WHERE SEND.RESTRICT_DT > NOW() - interval '5minute'
```
2.  시도 1:  아래처럼 작성하니 오류 발생 ($3 잘못된 syntax라며..)
```sql
SELECT COUNT(*)
FROM 2FACTOR_SEND SEND
WHERE SEND.RESTRICT_DT > NOW() - interval #{standardTime} || 'minute'
```

3. 해결 : cast를 사용해서 해결~
```sql
SELECT COUNT(*)
FROM 2FACTOR_SEND SEND
WHERE SEND.RESTRICT_DT > NOW() - CAST(#{standardTime} || 'minute' AS INTERVAL)
```

- CAST 사용법 : CAST는 데이터 타입을 변경하는 함수이다.

```sql
-- 숫자 -> 문자열
SELECT CAST(123 AS VARCHAR(10));

-- 문자열 -> 날짜
SELECT CAST('2024-10-10' AS DATE);
```
<br>

---
### 🟡 insert 후 바로 select를 했더니 NPE가 발생한다
- @Transactional을 붙이면 NPE가 발생한다.
- @Transactional을 안붙이고 디버깅하면 제대로 조회함.
- @Transactional을 안붙이고 그냥 실행하면 NPE발생함.
- insert후 바로 select 하는 법 : https://adjh54.tistory.com/406

__일단 해결__
- rw로 insert하고 ro로 select해서 위의 문제가 발생하였다.
- rw, ro는 추후 공부해봐야할듯❓❓❓

<br>

---
### 🟡 INSERT하고 키 반환하기 -  selectKey 사용법과 문제발생..
- __문제 발생__ : useGeneratedKeys, keyProperty. -> keyProperty에 pk값이 아니라 고객사번호(첫번째 컬럼)값이 반환이 된다. 

- __해결__ : keyColumn을 지정해줘야 한다. 첫번째 컬럼이 반환되는게 맞고, keyColumn값을 DB에 작성되어 있는 컬럼명과 동일하게 작성해야 한다! 대문자로 썼을 땐 에러 발생했음 ㅠㅠ 소문자로 쓰니까 됨...후...


- __사용법__
```xml
<insert id="createCert" parameterType="파라미터 타입" useGeneratedKes="true" keyProperty="certSn" keyColumn="certification_sn"
>
INSERT INTO ... VALUES ...
</insert>
```

<br>

---
### 🟡 @Transcational 제대로 알고 사용하자

- service 클래스 위에 @Transactional을 작성하였다. insert하고 바로 select하는 부분이 있었는데 select하는 곳에서 null이 반환이 되었다.
	- insert한 것이 아직 커밋하기 전이기 때문에 select일 때 반환이 안되는 것인가?


- 읽어보자.!!
	- https://velog.io/@eastperson/Transactional-%EC%83%81%ED%99%A9%EB%B3%84-commit-rollback-%EC%A0%84%EB%9E%B5
	- https://techblog.woowahan.com/2606/

	- https://medium.com/gdgsongdo/transactional-%EB%B0%94%EB%A5%B4%EA%B2%8C-%EC%95%8C%EA%B3%A0-%EC%82%AC%EC%9A%A9%ED%95%98%EA%B8%B0-7b0105eb5ed6

<br>

---
### 🟡 Enum 제대로 사용하기
- https://techblog.woowahan.com/2527/


<br>

---
### 🟡 Objects.requiredNonNull ??




<br>


---
### 🟡 UUID? JAVA에서 UUID 생성

- UUID를 사용하여 유일한 식별자를 생성할 수 있다.
- UUID의 구성
- 16진수 

<br>

---
### 🟡 HTTP 403 Forbidden
- BadRequestException : 400 에러
- BusinessException : 500 에러
- AuthorizationException : 403 에러 - Forbidden. 요청한 자원에 대한 권한이 없음.

<br>

---
### 🟡 objectMapper.convertValue, @JsonInclude
- 값이 null인건 응답값에 왜 안들어있지? 원래라면 restrictCode: "" 이렇게 들어가야 하는데 응답에 아예 restrictCode 값이 없다.

- @JsonInclude(Include,NON_NULL)

- @JsonProperty(access = JsonProperty.Access.WRITE_ONLY) 이건 뭐냐?

<br>

---
### 🟡 MyBatis 파라미터 사용법
```java
// 호출하는 부분
selectLoginRestrict(loginRestrictParam, "");
```
```sql
// xml 부분
SELECT *
FROM EXT
WHERE ctmmnyNo = #{loginRestrictParam.ctmmnyNo}
AND type = #{crtfcType}

```

<br><br><br><br>

## 🔴 Log-FO
### 🟡 document.getElementById("id").focus() 안되는 이유
- 원인
	- 요소가 아직 DOM에 렌더링되지 않았다면 focus()를 호출할 수 없다.
- 해결
	- $nextTick을 사용

```javascript
this.$nextTick(() => {
	const captchaEl = document.getElementById("captcha00");
	if (captchaEl) {
		captchaEl.focus();
	}
})
```
- $nextTick() : DOM이 업데이트된 후에 실행할 코드를 예약하는 것. DOM이 업데이트된 후에 실행됨.

<br>

---
### 🟡 axios error 핸들링 오류
- 문제 발생 상황 
	- api에서 ex 던졌는데 then, catch 둘 다 찍히고 response, error 객체에 undefined 찍힘;;;;
- 원인
	- 바로 ssgaxios.js 여기서 에러 코드 넣어줘야함. 왜인지는 모름;;
	- 응답이 ssgaxios.js 먼저 타고 들어옴.!!! 

<br>

---
### 🟡 watch 실행 & DOM 랜더링
1. 데이터 변경 : 컴포넌트의 데이터가 변경됨.
2. watchers : watch 실행.
3. Reactivity 시스템 : Vue의 반응형 시스템이 변경 사항 감지하고, 필요한 경우 DOM 재랜더링
4. Virtual DOM 업데이트 : Vue는 가상 DOM을 사용하여 변경된 내용을 계산하고, 실제 DOM을 최소한으로 업데이트함.

- DOM이 랜더링 된 후 함수를 실행하고 싶으면 $nextTick() 사용.

```javascript
/*
1. userSubInfo가 변경된다.
2. DOM이 업데이트 된다.
3. initSelected() 함수가 실행된다.
*/
watch: {
	'userSubInfo': function() {
		this.$nextTick(() => {
			this.inistSelected();
		})
	}
}

```

<br>

---
### 🟡created에선 DOM 접근이 어렵다
- created에서 팝업을 오픈하도록 코드를 짰다.
- created에선 DOM이 완전히 준비가 안되었기 때문에 직접적인 접근은 어렵다!!!
- 랜더링 이후에 DOM에 접근하려면 mounted에서 호출해야 한다!

<br><br><br>

## 🔴 NOTE

### 🟡 git --amend
- push 했을 땐 --amend 쓰지 마라;;;; 충돌 오지게 남;




## 🔴 REVIEW

### 🟡 필드 디폴트로 값줘라.

- 요구사항 : 제한이던 아니던 restrictCode는 값 넘겨줘라. 빈값 or 에러코드
- 처리 1
	-  api에서 @JsonInClude 삭제함.
	- 다시 리뷰 : 그걸 없애면 null 값인 필드 모두 보여줌. restrictCode만 보이게 해야함.

- 처리 2 
	- 난 생성할 때 set으로 빈 값으로 처리함.
	- 다시 리뷰: 그냥 필드 생성할 때 디폴트로 값 넣으면 된다.


<br>


## 🔴 Refactoring

### 🟡 ext에서 공통 필드 어떻게 처리할까?
- 빌더 패턴 사용할 수 있는 CommonModel2를 만들었다. 여기는 DB의 시스템 컬럼 담고있음.

- 로그인 파람 처리할때 mber랑 group 파람 묶어야 한다. restrictService에서 해당 파람 받아서 처리하고 싶어서.

- 그런데 CommonModel2, LoginParam을 둘 다 상속할 수 없다. 자바는 단일 상속이기 때문에...




