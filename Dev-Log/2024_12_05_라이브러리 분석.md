### 캡챠 라이브러리 적용
__google reCAPCHA v2, v3__ 또는 __cloudflare turnstile__ 적용
- 신세계 모바일 WEB(VUE + Spring Boot)에 적용
- 인증 방법/처리 프로세스 비교
- 필요 라이브러리 정리

---
## 🔴 google reCAPCHA, cloudflare 분석
### 🟡 현재 캡챠 사용 로직
- 어뷰징 회원 체크
- BO 
    - `shinsegaepoint_ext.mber_s_captcha`에 캡챠 정보 insert 후 비교
    - nl.captcha 사용.


### 🟡 google reCAPCHA
#### 점수를 사용하여 요청을 확인 (v3)
- https://github.com/abinnovision/vue-recaptcha-v3/tree/vue-v2.x

- 사용자를 방해하지 않고 각 요청에 대한 점수를 반환.
- 키 등록 사이트
    - https://www.google.com/recaptcha/admin/create?hl=ko

- 토큰은 2분 후 만료.

#### 보안문자를 사용하여 요청을 확인(v2)

 - "로봇이 아닙니다" 체크박스 or 표시되지 않는 reCAPTCHA 배지(백그라운드에서 요청확인)


#### 사용방법
1. 사이트에 https://www.google.com/recaptcha/admin/create?hl=ko 에서 reCAPTCHA 키를 생성.

    - 사이트 키 : HTML 코드에 사용

    - 시크릿 키 : reCAPTCHA api 백엔드에 사용.

2. reCAPTCHA v2 또는 v3 클라이언트 개발 - 사이트 키로 google에 토큰 요청 후 응답.

3. 사용자 응답 확인
    
    - https://www.google.com/recaptcha/api/siteverify


#### Vue.js + Spring Boot 적용
- Vue.js 필요한 라이브러리

- v2 : https://www.npmjs.com/package/vue-recaptcha/v/2.0.3?activeTab=readme

- v3 : https://www.npmjs.com/package/vue-recaptcha-v3


- 단점 : 구글이 점수를 지정하기 때문에 실제 사용자도 봇으로 판단되는 경우가 있음.



### 🟡 cloudflare turnstile

- 무료 : 위젯 수 최대 10개. 위젯당 10개의 호스트 네임만 유효성 지원

#### 모드
- 관리형
- 비대화형
- 보이지 않음

#### 문제
- 지원되지 않는 브라우저 : internet Explorer
    - 차장님 말씀 : 무슨 라이브러리 사용하면 explorer도 사용할 수 있다.


## 🔴 REVIEW
- 사용법이 중요한게 아니다. 사용법은 다 나온다. 기술적인 검토는 당연한거다.

- 중요한 건 지금 프로젝트에 적용할 수 있는지 판단하는 것이다. 또 FO에서 google 토큰 받고 BO로 던졌는데 거기서 에러나면 어떻게 처리할 건지 이런 것을 고려하는 것이 AA의 역할이다.
    1. 비동기인지, 동기인지 확인할 것 : 동기이면 응답이 안왔을 때 무한 대기에 빠질 수 있기 때문에 안좋음.(근데 뭐 요즘은 거의 다 비동기이니까.)

    2. 네트워크 부하 : restTemplate, http Client 등 다 패킷 사이즈가 다르다. 예전엔 패킷 사이즈에 따른 부하도 생각했어야 했다. 요즘에서야 컴퓨터 발전으로 생각안해도 된거지.
    3. FO에서 토큰 받았는데 BO에서 에러나면 어떻게 처리할 것인가? 실패했을 때 후처리로 본인인증을 할 건지, 그런 건 협업에서 결정하면 됨. 다만 이런 후처리가 필요하다는 것을 알려줘야 한다.
- 그럼 어떤 라이브러리를 선택해야할까? 고려사항은 다음과 같다.
    - 비용적인 측면
    - 사용자 친화적 - 우리가 편리하게 커스텀해서 사용할 수 있는 것은?
    - 사용할 npm 있는지 찾아볼 것. 없다면 CDN을 고려.


### 🟡 회고
- 명심 : 일 끝내놓고 놀자
- 목차
    1. google v2, google v3 기능 소개 & 필요한 라이브러리
    2. cloudflare 기능 소개 & 필요한 라이브러리
    3. 사용법 소개
    4. 고려할 사항 : 비용적인 측면, 도입 가능 여부, 사용자 친화적
    5. 선택할 라이브러리

<br><br><br><br>

## 🔴 Log-FO

- 상황 : mobile 파일 하나 복사해서 실행하려는데 실행이 안된다.
```bash

'vue-cli-service'은(는) 내부 또는 외부 명령, 실행할 수 있는 프로그램, 또는
배치 파일이 아닙니다.
npm ERR! code ELIFECYCLE
npm ERR! errno 1
npm ERR! shinsegaepointweb_mobile@0.4.0 serve-local: `vue-cli-service serve --mode local --inspect`
npm ERR! Exit status 1
npm ERR! 
npm ERR! Failed at the shinsegaepointweb_mobile@0.4.0 serve-local script.
npm ERR! This is probably not a problem with npm. There is likely additional logging output above.

```

- 해결 : node_modules 삭제 후 다시 설치





<br><br><br><br>

## 🔴 Log-BO

### 🟡 ParameterizedTypeReference<T> ??
- RestTemplate 같은 클래스에서 제네릭 타입을 응답받을 때 유용하게 쓰인다.

- Java에서는 타입 소거(Type Erasure)때문에 런타임에 제네릭 타입을 알 수 없다.

```java
RestTemplate restTemplate = new RestTemplate();

// 런타임 때 List<String> ---> List 가 된다.
List<String> response = restemplate.getForObject(url, List.class);
```
- 제네릭 정보가 사라지면 타입이 안전하지 않다. `따라서 ParameterizedTypeReference<T>`를 사용해야 한다.


```java
RestTemplate restTemplate = new RestTemplate();

ResponseEntity<List<String>> response = restTemplate.exchange(
    url,
    HttpMethod.GET,
    null,
    new ParameterizedTypeReference<List<String>>() {}
);

List<String> list = response.getBody();
```


<br>

---

### 🟡 field serialize 관련 annotion 정리

- Spring에선 기본적으로 Jackson을 사용하여 직렬화, 역직렬화를 한다.


- 값이 null이면 값이 누락될 수 있다. ex) `{ "name" }`

#### 1. `@JsonInclude(JsonInclude.Include.NON_NULL)`

- 해당 필드의 값이 NULL이면 JSON에 포함하지 않는다.


#### 2. LocalDateTime 직렬화

- `@DataTimeFormat`, `@JsonFormat` 사용.


#### 3. `@JsonProperty`

- `@JsonProperty("error-codes")` : error-codes 키를 해당 필드와 매핑한다.

- `@JsonProperty(access = JsonProperty.Access.WRITE_ONLY)` : 역직렬화만 가능. String -> Object만 가능. 즉 요청에서 읽기는 가능하고 응답 결과엔 표시 안된다.


