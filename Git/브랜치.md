❗ 커밋이 하나도 없으면 브랜치 생성이 안된다 ❗
__
### 브랜치 생성
```
git brach test
```
### 브랜치 생성 동시에 이동
```
git switch -c test
```
### 브랜치 삭제
```
git branch -d test
```
### 브랜치 이름 변경
```
git branch "기존 브랜치명" "새 브랜치명"
```
### 여러 브랜치 내역 한꺼번에 보기
```
git log --all -decorate --oneline --graph
```

