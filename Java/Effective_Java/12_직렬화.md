### 🍎 Item85. 자바 직렬화의 대안을 찾으라

```
직렬화는 위험하다. 시스템 밑바닥부터 설계한다면 JSON이나 프로토콜 버퍼같은 대안을 사용하라.
신뢰할 수 없는 데이터는 역직렬화하지 말자. 꼭 해야한다면 객체 역질렬화 필터를 사용하되, 이마저도 모든 공격을 막아줄 수 없음을 기억하라.
```

### 🍎 Item86. Serializable을 구현할지는 신중히 결정하라
```
노출될 가능성이 없거나, 보호된 환경에서만 쓰일 데이터가 아니라면 Serializalbe 구현은 신중히 해야 한다.
```

### 🍎 Item86. 커스텀 직렬화 형태를 고려해보라
```
클래스를 직렬화하기로 결정했다면 커스텀 직렬화 형태를 고안하라.
자바의 기본 직렬화 형태는 직렬화한 데이터가 해당 객체의 논리적 표현에 부합할 때만 사용하라.

```
### 🍎 Item87. readObject 메서드는 방어적으로 작성하라
```
- private인 객체 참조 필드는 필드가 참조하는 객체를 방어적 복사하라. 불변 클래스의 가변 요소가 여시 속한다???
- 방어적 복사 다음에는 반드시 불변식 검사를 해야한다.
- 역직렬화 후 객체 그래프 전체의 유효성을 검사해야 한다면 ObjectInputValidation 인터페이스를 사용하라.
- 재정의할 수 있는 메서드는 호출하지 말자.
```

### 🍎 Item88. 인스턴스 수를 통제해야 한다면 readResolve보다는 열거 타입을 사용하라.
```
불변을 지키기 위해 인스턴스를 통제해야 한다면 열거타입을 사용하라.

```


### 🍎 Item89. 직렬화된 인스턴스 대신 직렬화 프록시 사용을 검토하라.

```
제 3자가 확장할 수 없는 클래스라면 가능한 한 직렬화 프록시 패턴을 사용하자.
이 패턴이 불변식을 안정적으로 직렬화해주는 가장 쉬운 방법일 것이다.
```



