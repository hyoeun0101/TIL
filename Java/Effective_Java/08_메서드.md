- 메서드 설계할 때 주의할 점.
- 매개변수와 반환값을 어떻게 처리해야하나
- 메서드 시그니처는 어떻게 설계해야하나
- 문서화는 어떻게 해야하나
- 사용성, 견고성, 유연성을 고려하여 메서드 설계하기

## 🍎 Item 49. 매개변수가 유효한지 검사하라.
```
메서드나 생성자의 매개변수에 어떤 제약이 있을지 생각해야 한다. 그 제약을 문서화하고, 메서드 코드 시작 부분에서 유효성 검사를 해야 한다.
```
- 메서드나 생성자의 매개변수의 값이 특정 조건을 만족해야 한다.(인덱스 값은 음수X, 객체 참조는 null X) 그 특정 조건은 반드시 문서화해야 하며, 메서드의 핵심 로직이 시작되기 전에 검사를 해야 한다.
- public, protected 메서드에선 올바른 매개변수가 아닐 때 던지는 예외를 문서화해야 한다. @throws 자바 독 태그를 사용한다.
```java
/**
 * @param m 계수(양수여야 한다.)
 * @return 현재 값 mod m
 * @throws ArithmeticException m이 0보다 작으면 발생한다.
 */
public BigInteger mod(BigInteger m) {
    if(m.signum() <= 0) {
        throw new ArithmeticException("계수(m)는 양수여야 합니다." + m);
    }
    ...
}

``` 

## 🍎 Item50. 적시에 방어적 복사본을 만들어라.
```
클라이언트로부터 받는 요소, 클라이언트에게 반환하는 요소가 가변이라면 그 요소는 반드시 방어적 복사를 해야 한다.
단 복사 비용이 너무 크거나 클라이언트가 그 요소를 신뢰한다면 방어적 복사 대신 그 요소를 수정했을 때의 책임은 클라이언트에게 있음을 문서에 명시해라.
```
### 방어적 복사?
```java
// 불변으로 보여도 변경될 수 있음.
public final class Period {
    private final Date start;
    private final Date end;

    public Period(Date start, Date end) {
        if(start.compareTo(end) > 0) {
            throw new IllegalArgumentException(start+" after "+end);
        }
        this.start = start;
        this.end = end;
    }
    public Date getStart() {return start;}
    public Date getEnd() {return end;}
}

...

Date start = new Date();
Date end = new Date();

Period p = new Period(start, end);
end.setYear(78); //p의 내부를 수정했다!
```
- 방어적 복사하기
```java
public Period(Date start, Date end) {
    this.start = new Date(start.getTime());
    this.end = new Date(end.getTime());

    if(start.compareTo(end) > 0) {
        throw new IllegalArgumentException(this.start+" after "+this.end);
    }
}

...

Date start = new Date();
Date end = new Date();
Period p = new Period(start, end);
p.end.setYear(78); // 역시나 p의 내부를 변경할 수 있다!
```
- 필드를 반환할 때도 방어적 복사를 사용한다. 이제 Period는 완벽한 불변!
```java
public Date getStart() {
    return new Date(start.getTime()); 
}

public Date getEnd() {
    return new Date(end.getTime());
}
```
## 🍎 Item51. 메서드 시그니처를 신중히 설계하라.
```

```

## 🍎 Item52. 오버라이딩은 신중히 사용하라.

## 🍎 Item53. 가변인수는 신중히 사용하라.
```
인수 개수가 일정하지 않은 
```