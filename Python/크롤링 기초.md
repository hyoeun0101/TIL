```
from selenium import webdriver
from bs4 import BeautifulSoup

browser = webdriver.Chrome('./chromedriver')
url ='url 넣기'
browser.get(url)

html = browser.page_source

# html 문서 읽어오기
soup = BeautifulSoup(html, 'html.parser')

test = soup.select('태그')
# 부모태그 > 자식태그
# 결과값은 list

# 0번째의 텍스트만 가져오기
print(test[0].text)


# 태그 안의 속성값 가져오기
print(test[0]['src'])


# 엑셀 파일로 저장
import pandas as pd
# 행으로 들어갈 데이터=> results =  [  [category, title, img, link] , [ ... ], [ ... ] , ...  ] 
df = pd.DataFrame(results)

df.columns = ['카테고리', '프로그램명', '이미지', '링크']

df.to_excel('파일명.xlsx', index=False)
# index=False => row에 인덱스 없애기

```
![image](https://user-images.githubusercontent.com/96059261/157273228-ca99215c-b0a1-4dc5-9c19-83d350a861f9.png)

![image](https://user-images.githubusercontent.com/96059261/157273426-ec4d80ea-a861-4fbb-8590-c48811ebba68.png)
## 셀레니움 셀럭트
```
find_elements_by_css_selector('조건')
```
