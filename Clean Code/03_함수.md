## 함수 잘 만드는 법

### 1. 작게 만들어라.
- if/else문, while문 등에 들어가는 블록은 한 줄이어야 한다. 대개 거기서 함수를 호출한다. 중첩 구조가 생길만큼 함수가 커져선 안된다!

### 2. 한가지만 해라.
- 그 한 가지가 무엇인데?? 추상화 수준의 한 가지. 
- 의미 있는 다른 이름으로 함수를 추출할 수 있다면 그 함수는 여러 작업을 하고 있는 것이다.