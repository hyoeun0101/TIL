### 원격 프로그램 실행
- 브라우저에서 URL 호출하면 톰캣(WAS)가 객체를 생성(Refletction 사용해서)해주고 그 메서드를 호출함.

## 🍎 클라이언트와 서버
- 만약 프로그램을 클라이언트에 설치한다면? 클라이언트에서 프로그램을 다운받고, 업데이트 시 직접 업데이트해야 하는 번거러움이 있다. 
- WAS에 프로그램을 설치하고 클라이언트의 응답에 답해주기만 하면 된다.
