# 1. 실습
### 1. xml로 빈 등록하기
src/main/resources > New> XML Configuration File > Spring Config   
STS에서는 src/main/resources> new > Spring Bean Configuration File

config.xml
```xml
<?xml version="1.0" encoding="UTF-8"?>
<beans>
<!-- 사용할 빈 등록하기 -->
    <bean id="car" class="com.hyocoding.ch2.Car"/>
    <bean id="engine" class="com.hyocoding.ch2.Engine"/>
    <bean id="door" class="com.hyocoding.ch2.Door"/>
</beans>
```
   
```java
package com.hyocoding.ch2;

import org.springframework.context.ApplicationContext;

class Car{}
class Engine{}
class Door{}

public class SpringDiTest{
    public static void main(String[] args){
        ApplicationContext ac = new GenericXmlApplicationContext("config.xml");
        //by Name
        Car car = (Car)ac.getBean("car");
        //Car car = ac.getBean("car",Car.class);
        Engine engine = (Engine)ac.getBean("engine");
        Door door = (Door)ac.getBean("door");

        System.out.println("car="+car);
    }
}
```
-> 싱글톤.getBean()은 같은 객체 반환.   

다른 객체 생성하려면 xml 파일수정.   
<bean id="car" class="com.hyocoding.ch2.Car" scope="prototype"/>   
디폴트는 scope="singleton"

```java
class Car{
    String color;
    int oil;
    Engine engine;
    Door[] doors;
    //toString, setter 작성. 
}
```

```java
car.setColor("red");
car.setOil(100);
car.setEngine(engine);
car.setDoors(new Door[]{ac.getBean("door",Door.class), ac.getBean("door",Door.class)});
```
setter 호출대신 xml에 property를 설정함.

```xml
 <bean id="door" class="com.hyocoding.ch2.Door">
    <property name="color" value="red"/>
    <property name="oil" value="100"/>
    <property name="engine" ref="engine"/>
    <property name="doors">
        <ref bean="door"/>
        <ref bean="door"/>
    </property>
</beam>
```
기본형,String -> value=" "    
참조형 -> ref   
property 태그가 setter를 호출하여 초기화하는 것임. 그래서 setter 작성해야함.   
생성자를 이용해서 초기화하는 코드는 constructor-arg태그 사용
```xml
 <bean id="door" class="com.hyocoding.ch2.Door">
    <constructor-arg name="color" value="red"/>
    <constructor-arg name="oil" value="100"/>
    <constructor-arg name="engine" ref="engine"/>
    <constructor-arg name="doors">
        <ref bean="door"/>
        <ref bean="door"/>
    </constructor-arg>
</beam>
```

### 2. component scan
- 빈 등록하기
```xml
<context:component-scan base-package="com.hyocoding.ch2"/>
```
빈으로 등록할 클래스에 @Component 붙여주기   
   
```xml
<context:component-scan base-package="com.hyocoding.ch2">
    <context:exclude-filter type="regex" expression="com.hyocoding.ch2.diCopy*.*"/>
</context:component-scan>
```
```java
@Value("red")
String color;
@Value("100") int oil;
@Autowired Engine engine;
@Autowired Door[] doors;
```
<context:annotation-config/> Autowired 쓰려면 이거 작성 

같은 타입이 여러개. getBean하면 에러
이럴 땐 by Name으로.
@Autowired는 타입으로 먼저 검색 후, 여러 개면 이름으로 검색한다.

```java
@Autowired //by Type 후 여러 개면
@Qualifier("superEngine")//by Name
Engine engine;

@Resource(name="superEngine")//by Name
Engine engine;
```

# 2. DI 이론

1. 빈 이란?
- Spring Container가 관리하는 객체
- Spring Container : 빈 저장소, 빈을 저장, 관리
2. BeanFactory 
: 빈을 생성, 연결
3. ApplicationContext 
: BeanFactory를 확장해서 여러 기능 추가 정의. 모든 환경을 관리하는 것.   
== Spring Container   
4. Root ApplicationContext와 Servlet ApplicationContext

web.xml에   
<listener-class>org.springframework.web.context.CotnextLoaderListener</listener-class>   
이벤트 처리기-> XmlWebApplictionContext를 생성함. 톰켓이 시작할 때 이벤트를 체크해서 XmlWebApplicationContext를 생성함. 설정은 root-context.xml   
Dispatcher Servelt을 서블릿으로 등록. 설정 파일은 servlet-context.xml   
bean을 찾을 때 자식 찾은 후 부모로.