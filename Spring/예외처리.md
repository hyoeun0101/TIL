### 스프링에서 예외처리
1.  try-catch 문 사용

2. 컨트롤러에 @ExceptionHandler로 메서드 처리

3. @ControllerAdvice 클래스의 @ExceptionHandler로 메서드 처리

4. 예외 종류별로 뷰 지정하기

5.  statuscode별로 뷰 지정하기


# @ExceptionHandler
예외처리를 위한 메서드에 @ExceptionHandler를 붙인다.

```java
@Controller
public class ExceptionController{
    //처리할 예외가 여러 개면 배열로 작성.
    //@ExceptionHandler({NullPointerException.class, FileNotFoundException.class})
    @ExceptionHandler(Exception.class)
    public String catcher(Exception ex, Model m){
        m.addAttribute("ex",ex);
        return "error";
    }

    @RequestMapping("/ex")
    public String main(Model m)throws Exception{
        m.addAttribute("msg","message for ExceptionController.main()");
        //예외 발생
        throw new Exception("예외 발생");
    }
}

```
__!!주의!!__
catcher의 모델과 main의 모델은 서로 다른 객체.   
   
### error.jsp 에서 ex로 예외 출력하기  
```xml
발생한 예외: ${ex}   
예외 메세지 : ${ex.message}   
<c:forEach items="${ex.stackTrace}" var="i">
	<li>${i.toString()}</li>
</c:forEach>
```

### isErrorPage=true
error.jsp에서   
<%@ page isErrorPage=true %>를 주면 ex를 모델에 추가하지 않아도 된다.
```xml
<%@ page isErrorPage="true" %>
발생한 예외: ${pageContext.exception}   
예외 메세지 : ${pageContext.exception.message}   
<c:forEach items="${pageContext.exception.stackTrace}" var="i">
	<li>${i.toString()}</li>
</c:forEach>
```

### @ControllerAdvice
전역 예외 처리 클래스 작성.   
@ControllerAdvice("com.hyocoding.ch1") 이렇게 지정된 패키지에만 적용 가능.   

더 가까운 @ExceptionHandler에서 처리함.

### @ResponseStatus
응답 메세지의 상태 코드 변경하기.

@ResponseStatus(HttpStatus.BAD_REQUEST) 400으로 변경.

### statuscode별 페이지 처리하기
1. web.xml에 추가   
```xml
<error-page>
    <error-code>400</error-code>
    <location>/error400.jsp</location>
</error-page>

<error-page>
    <error-code>500</error-code>
    <location>/error500.jsp</location>
</error-page>
```

2. WEB-INF에 jsp추가
error400.jsp
```xml
<%@ page isErrorPage="true" %>
[400] 잘못된 요청입니다.
```

### 예외 종류별 페이지 처리하기
1. servlet-context.xml에 등록

```xml
<beans:bean class="org.springframework.web.servlet.handler.SimpleMappingExceptionResolver">
    <beans:property name="defaultErrorView" value="error"/>
    <beans:property name="exceptionMappings">
        <beans:props>
            <beans:prop key="com.hyocoding.ch1.MyException">error400</beans:prop>
        </beans:props>
    </beans:property>
    <beans:property name="statusCodes">
        <beans:props>
            <beans:prop key="error400">404</beans:prop>
        </beans:props>
    </beans:property>
</beans:bean>
```

MyException이 발생하면 404로 처리   
/views/error400.jsp 추가   
error400.jsp에서 isErrorPage=true이면 디폴트로 500임.   


### 예외 처리 과정

클라이언트 요청 -> DispatcherServlet -> Controller (! 예외발생 !) -> DispatcherServlet -> handlerExceptionResolvers   
   
handlerExceptionResolver 에선 다음의 우선순위로 예외를 처리한다.    
   
1. ExceptionHandlerExceptionResolver      
   
2. ResponseStatusExceptionResolver   
   
3. DefaultHandlerExceptionResolver   
   
파일 찾아보기   
maven > spring-webmvc > org.springframework.web.servlet > DispatcherServlet.properties   


